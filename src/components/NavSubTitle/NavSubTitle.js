import React from 'react'
import './NavSubTitle.css'

const NavSubTitle = ({ title }) => (
  <div className="row text-center">
    <h2 className="nav-subtitle">{title}</h2>
  </div>
)

export default NavSubTitle